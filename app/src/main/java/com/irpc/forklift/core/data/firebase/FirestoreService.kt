// 📁 core/data/firebase/FirestoreService.kt
package com.irpc.forklift.core.data.firebase

/**
 * 🔥 Firestore Service — Wrapper สำหรับ query ที่ใช้บ่อย
 *
 * - รับ/ส่ง document
 * - Query collection ด้วย filter
 * - Offline persistence
 */
object FirestoreService {
    // suspend fun <T> getDocument(path: String, clazz: Class<T>): Result<T>
    // suspend fun <T> getCollection(path: String, clazz: Class<T>): Result<List<T>>
    // suspend fun setDocument(path: String, data: Any): Result<Unit>
}
