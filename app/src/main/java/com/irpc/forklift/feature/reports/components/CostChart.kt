// 📁 feature/reports/components/CostChart.kt
package com.irpc.forklift.feature.reports.components

import androidx.compose.foundation.layout.*
import androidx.compose.material3.*
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

/**
 * 💰 Cost Chart Placeholder
 *
 * For production, use MPAndroidChart or Vico
 *
 * @Composable
 * fun CostChart(
 *     data: Map<String, Int>,  // month → total cost
 *     modifier: Modifier = Modifier,
 * ) {
 *     AppCard(modifier = modifier) {
 *         Text("ค่าใช้จ่ายซ่อมบำรุงรายเดือน", style = MaterialTheme.typography.titleMedium)
 *         Spacer(Modifier.height(8.dp))
 *         // Placeholder — TODO: implement chart
 *         Surface(
 *             modifier = Modifier.fillMaxWidth().height(200.dp),
 *             color = MaterialTheme.colorScheme.surfaceVariant,
 *         ) {
 *             Box(contentAlignment = androidx.compose.ui.Alignment.Center) {
 *                 Text("📊 Chart Placeholder")
 *             }
 *         }
 *     }
 * }
 */
object CostChart {
    // TODO: implement
}
