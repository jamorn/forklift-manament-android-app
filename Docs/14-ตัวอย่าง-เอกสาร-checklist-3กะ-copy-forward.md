# 14 — ตัวอย่างเอกสาร Checklist ทั้ง 3 กะ ใน 1 วัน (พร้อม Copy-Forward)

> เอกสารนี้จำลองหน้าตา Doc ของแต่ละกะ (M / E / N) ใน **work-day เดียว (02 ส.ค.)**
> เพื่อให้เห็นว่า ระบบ "Copy-Forward" (pre-fill จากกะก่อน) ส่งผลต่อเอกสารแต่ละกะอย่างไร
>
> **สมมุติฐาน:** ตรวจรถ 1 คัน (FL-101) เพื่อให้ตัวอย่างกระชับ ระบบแยก Doc ต่อกะ ตาม Docs/13
>
> **สถานะ:** 📝 ตัวอย่างจำลองเท่านั้น ยังไม่ได้ implement

---

## 🧩 หลักการ Copy-Forward (ระบบที่มีอยู่)

จาก `ChecklistViewModel` (~บรรทัด 104-111):
```kotlin
// Copy-Forward: pre-fill results + remarks + meter จากกะก่อน
... copy(cs.remarks)
... mainRemark = cs.main_remark,
... meter = ...
```

เมื่อ operator เริ่มกะใหม่ ระบบ **copy ค่าจากกะก่อนหน้า** ขึ้นต้นให้:
| ฟิลด์ | copy จากกะก่อน หรือไม่ | เหตุผล |
|-------|----------------------|--------|
| `manhourMeter` | ✅ copy | กะถัดไปใช้เลขต่อจากกะก่อน (สะสมไมล์) |
| `main_remark` | ✅ copy แล้ว **overwrite** ได้ | เป็น **สภาพปัจจุบัน** ของรถ ณ กะนี้ |
| `remarks` (ต่อ item) | ✅ copy | เห็นสภาพเดิม/ที่กะก่อน fail (เป็น baseline) |
| `results` (pass/fail) | ✅ copy (แก้ได้) | เห็นผลเดิม แล้ว operator ตรวจของตัวเองใหม่ได้ |

---

## 🎯 หลักการสำคัญ 1: `main_remark` = "สภาพปัจจุบัน" (overwrite ถูกต้อง)

**เหตุผลทางธุรกิจ (จากผู้ใช้งาน):**
> กะ E ตรวจพบรอยร้าวกระจก → `unsafe` → **นำรถไปซ่อม**
> กะ N มารับกะ → รถ**ซ่อมเสร็จแล้ว กลับมา normal** → `main_remark` ใหม่จึงแทนที่ของเก่า

ดังนั้น:
- `main_remark` ต้องสะท้อน **"สภาพรถ ณ ตอนที่กะนี้ตรวจ"** (ปัจจุบัน) → **overwrite ถูกต้อง**
- รถซ่อมเรียบร้อย → `main_remark` = "ซ่อมแล้ว ปกติ", `status = normal` ✅

---

## 🎯 หลักการสำคัญ 2: เรื่อง "ไล่บี้คนทำ" / รูป / ซ่อม → ย้ายไป supervisor + LINE (ไม่ใช่งานระบบ)

**ตาม workflow จริง:**
> เมื่อพบปัญหา/ความเสียหาย (เช่น แก้วแตก) → operator **ถ่ายรูปลง LINE กลุ่ม**
> และปัญหานี้จะ**ย้ายไปให้ supervisor จัดการ** (แนบรูปว่ารถคันไหนเสียหายอะไร + ใส่หมายเหตุ)
> หัวหน้าไปไล่บี้หาคนทำตามรูป/หลักฐานใน LINE

**ผลต่อระบบ checklist:**
- ระบบ **ไม่ต้อง** รับผิดชอบ "การไล่บี้ / ประวัติการซ่อม / ว่าใครทำ"
- `main_remark` ไม่ต้องเก็บประวัติกะแบบ append (ไม่ต้องเก็บว่า "ใครเจอตอนไหน")
- ✅ ยืนยันความเรียบง่าย: checklist แค่บอก **สภาพปัจจุบัน** ของรถแต่ละกะเท่านั้น
- supervisor/ฝ่ายซ่อม ใช้ **LINE + รูป** เป็นหลักฐานจัดการคนละส่วน

---

## 🗂️ ภาพรวม: 3 Docs ของ work-day 02 ส.ค.

```
work-day (02 ส.ค.)
├── Doc กะ M  (06:00–14:00)   → date=2026-08-02, shift=M  (สภาพปกติ)
├── Doc กะ E  (14:00–22:00)   → date=2026-08-02, shift=E  (พบรอยร้าว → ไปซ่อม)
└── Doc กะ N  (22:00–06:00)   → date=2026-08-02, shift=N  (ซ่อมเสร็จ → กลับ normal)
```

> `date` ทั้ง 3 กะ = **02 ส.ค. เดียวกัน** (ใช้ work date) แต่ `shift` ต่างกัน + สมาชิกต่าง document
> 📌 กรณีพบปัญหาที่ต้องซ่อม → operator ถ่ายรูปลง LINE + แจ้ง supervisor (คนละส่วนกับ checklist)

---

## 📄 Doc ที่ 1 — กะ M (เช้า) 06:00–14:00

เริ่มกะแรกของวัน → **ไม่มี copy-forward** (เริ่มต้นเปล่า)

```
DailyChecksheet(FL-101, กะ M)
┌─────────────────────────────────────────────────────┐
│ วันที่      : 2026-08-02  (work-day)  กะ: M          │
│ FL No.     : FL-101                                  │
│ ผู้ตรวจ    : wiroj (operator)                        │
│ ...................... checklist items ............  │
│   ✔ ครัชน้ำมัน      : PASS   (กะ M ตรวจปกติ)         │
│   ✔ ล้อ / ยาง       : PASS                           │
│   ✔ ไฟหน้า         : PASS                           │
│   ✔ กระจก          : PASS  ← (ยังไม่มีปัญหา)         │
│   ⚠ manhour meter  : 12,450  กม.                    │
│ ...................... main_remark ................ │
│   main_remark      : ""  → ตรวจปกติทุกอย่าง          │
│   status           : normal                          │
└─────────────────────────────────────────────────────┘
  → main_remark=""
  → remarks={} , results={...pass}, status=normal
```

---

## 📄 Doc ที่ 2 — กะ E (บ่าย) 14:00–22:00

**Copy-Forward จากกะ M** → เห็น baseline ของกะ M แต่ตรวจของตัวเองใหม่

```
DailyChecksheet(FL-101, กะ E)
┌─────────────────────────────────────────────────────┐
│ วันที่      : 2026-08-02  (work-day)  กะ: E          │
│ FL No.     : FL-101                                  │
│ ผู้ตรวจ    : somchai (operator)                      │
│ ...................... checklist items ............  │
│   ✔ ครัชน้ำมัน      : PASS   (copy M → ยังปกติ)      │
│   ✔ ล้อ / ยาง       : PASS                           │
│   ✔ ไฟหน้า         : PASS                           │
│   ✔ กระจก          : FAIL  "พบรอยร้าวกระจกหน้า"     │
│   ⚠ manhour meter  : 12,480  กม.  (copy M + อ่านเพิ่ม)│
│ ...................... main_remark ................ │
│   main_remark      : "พบรอยร้าวกระจกหน้า → ส่งซ่อม"   │
│   status           : unsafe  (พบปัญหา → ไปซ่อม)      │
└─────────────────────────────────────────────────────┘
  → main_remark="พบรอยร้าวกระจกหน้า → ส่งซ่อม"
  → remarks={กระจก:"รอยร้าวหน้า"}, results={กระจก:fail}, status=unsafe
  📌 ลูกทีมถ่ายรูปลง LINE + แจ้ง supervisor ไปด้วย (คนละส่วน)
```

---

## 📄 Doc ที่ 3 — กะ N (กลางคืน) 22:00–06:00

**Copy-Forward จากกะ E** → เห็น unsafe/ซ่อม ของกะ E แต่กะ N ตรวจแล้วรถกลับปกติ

```
DailyChecksheet(FL-101, กะ N)
┌─────────────────────────────────────────────────────┐
│ วันที่      : 2026-08-02  (work-day)  กะ: N          │
│            ↑ ข้ามเที่ยงคืนแล้ว แต่ยังเป็นของ work-day 02 │
│ FL No.     : FL-101                                  │
│ ผู้ตรวจ    : anan (operator, กะดึก)                  │
│ ...................... checklist items ............  │
│   ✔ ครัชน้ำมัน      : PASS   (copy E)                │
│   ✔ ล้อ / ยาง       : PASS                           │
│   ✔ ไฟหน้า         : PASS                           │
│   ✔ กระจก          : PASS  → "ซ่อมเรียบร้อยแล้ว"     │
│   ⚠ manhour meter  : 12,520  กม.  (copy E + อ่านเพิ่ม)│
│ ...................... main_remark ................ │
│   main_remark      : "ซ่อมกระจกแล้ว กลับมาใช้ปกติ"    │
│   status           : normal   (overwrite จาก unsafe) │
└─────────────────────────────────────────────────────┘
  → main_remark="ซ่อมกระจกแล้ว กลับมาใช้ปกติ"
  → remarks={} , results={...pass}, status=normal   ← กลับมา normal
```

> ✅ **overwrite ที่ถูกต้อง:** รถซ่อมเสร็จ → `main_remark` ใหม่แทนที่ของเก่า → `status=normal`

---

## 🔁 สรุปการ copy-forward ระหว่าง 3 กะ

| ฟิลด์ | M (แรก) | E (copy จาก M) | N (copy จาก E) |
|-------|---------|----------------|----------------|
| `manhourMeter` | 12,450 | 12,480 (copy + อ่านเพิ่ม) | 12,520 (copy + อ่านเพิ่ม) |
| `main_remark` | "" | "พบรอยร้าว → ส่งซ่อม" | "ซ่อมแล้ว กลับปกติ" **(overwrite)** |
| `remarks` | {} | {กระจก:รอยร้าว} | {} (เคียร์แล้ว ซ่อมเสร็จ) |
| `results` | all pass | กระจก→fail | all pass |
| `status` | normal | unsafe → ส่งซ่อม | **normal** (กลับมา) |

**จุดสำคัญ:**
- `manhourMeter` — copy-forward แล้ว**เพิ่มต่อ** ถูกต้อง กะต่อเนื่องกัน
- `main_remark` — **overwrite** ถูกต้อง เพราะสะท้อน **สภาพปัจจุบัน** ของรถ กะต่อกะ
- **เรื่อง "ไล่บี้/รูป/ซ่อม"** — ย้ายไป **supervisor + LINE กลุ่ม** คนละส่วน ไม่ใช้ระบบ checklist

---

## 📝 ข้อสรุปแนวทางที่ถูกต้อง (สอดคล้อง Docs/13 + business logic)

1. **`manhourMeter`** → copy จากกะก่อน + operator อ่านเลขใหม่ (สะสมต่อ)
2. **`main_remark`** → copy มาเป็น baseline แล้ว **operator ตรวจ/แก้ของกะตัวเองได้ (overwrite)** — สื่อ "สภาพปัจจุบัน"
3. **`results`/`remarks`** → copy มา baseline แล้วแก้เป็นของกะตัวเอง
4. **`status`** → เปลี่ยนตามสภาพจริงของแต่ละกะ (normal → unsafe → ซ่อม → normal)
5. **งาน "ไล่บี้คนทำ" + รูป + การซ่อม** → **อยู่ในส่วนของ supervisor / LINE กลุ่ม** ไม่ใช่งานระบบ checklist
6. ใช้ **work date** (Docs/13) → 3 กะของคืนเดียวกัน `date=02` ตรงกัน

---

> **หมายเหตุ:** เอกสารนี้เป็นเพียง **ตัวอย่างจำลอง** แสดงแนวคิดของเอกสาร 3 กะ + copy-forward
> เพื่อใช้ออกแบบก่อน implement จริง ยังไม่มีการแก้ไขโค้ด

---

## 🔄 อัปเดตให้สอดคล้องข้อตกลงใหม่ (ดู Doc 16 ประกอบ)

> ❗ เอกสารนี้ถูก **อัปเดต** ให้ตรงกับข้อตกลงล่าสุด (Doc 16 — Design Decision Log)
> แนวคิด copy-forward / work-date / 3 กะ / main_remark overwrite ยังถูกต้องตามเดิม
> แต่ต่อไปนี้เป็น**สิ่งที่เพิ่ม/ปรับใหม่** จากการตกลงกันล่าสุด

### ፎ. สิ่งที่ "ตกลงใหม่ไปแล้ว" ที่มีผลกับเอกสาร 3 กะ

| หัวข้อ | ข้อตกลงใหม่ (Doc 16) | ผลต่อ 3 กะ |
|--------|----------------------|------------|
| **`date`** | ใช้ **work date** (ISO +07:00) — กะ N หลังเที่ยงคืน = วันเริ่มกะ | 3 กะของคืนเดียวกัน `date` ตรงกัน (ตามเดิมถูกต้อง) ✅ |
| **`created_at`/`updated_at`** | **String ISO 8601 + timezone `+07:00`** (user ไม่เห็น ทำงานเบื้องหลัง) | ทุก doc ต้องมี `created_at`; เมื่อแก้ไขเพิ่ม `updated_at` |
| **`id`** | Firestore **auto-gen** (create) + **update ตาม id เดิม** (ไม่สร้างซ้ำ) | แต่ละ doc ของแต่ละกะมี id แยก (M, E, N) แล้วแก้ตาม id เดิม |
| **item checklist** | ยังไม่ครอบคลุม — รอข้อมูลบริษัทให้เช่ารถ + เพิ่ม `order`/`enabled` อนาคต | ตัวอย่างนี้ใช้ item ชั่วคราวได้ แต่รายการจริงต้องเพิ่ม/ปรับภายหลัง |
| **Firebase** | ใช้ **emulator จำลองก่อน** ยังไม่ผูกจริง | ตัวอย่างนี้เป็น mock — ยังไม่ส่งขึ้น firebase จริง |

### 🕒 ตัวอย่าง: 3 กะ พร้อม timestamp + id (updated)

```
Doc กะ M : id=auto1,  date=2026-08-02, shift=M,
          created_at="2026-08-02 06:10:00 +07:00", status=normal
Doc กะ E : id=auto2,  date=2026-08-02, shift=E,  (copy จาก M)
          created_at="2026-08-02 14:05:00 +07:00", status=unsafe
Doc กะ N : id=auto3,  date=2026-08-02, shift=N,  (copy จาก E)
          created_at="2026-08-02 22:10:00 +07:00", status=normal
```

📌 แต่ละกะ = **คนละ document id** (แยกกะ/แยก crew) — แก้ไขตาม id เดิม, `updated_at` เปลี่ยนเมื่อถูกแก้

### 📝 สรุป: สิ่งที่ Doc 14 นี้ใช้ได้จริง vs ต้องพิจารณาใหม่
- ✅ **ใช้ได้จริง:** copy-forward (manhour, results, remarks) / main_remark overwrite / status เปลี่ยนตามสภาพ / work date 3 กะตรงกัน
- ⏳ **ต้องรอ:** item checklist ให้ครบ (บริษัทเช่า) + เพิ่ม `order`/`enabled`
- 🔧 **พ่วงข้อตกลงใหม่:** `created_at`/`updated_at` (ISO+07:00), `id` auto-gen/update ตาม id เดิม → ดู Doc 16

---

> **หมายเหตุ ณ บันทึกนี้:** Doc 14 นี้ถูกอัปเดตให้สอดคล้อง Doc 16 แล้ว แนวคิดหลัก (copy-forward/3กะ/work-date) ยังเป็นแนวทางในการออกแบบ ยังไม่ implement
